apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  ports:
  - name: result
    port: 80
  selector:
    app: result

